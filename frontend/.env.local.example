# URL of the FastAPI backend for local development.
# The frontend dev server runs on port 3000; the backend runs on port 8000.
# When running via Docker (./scripts/start-mac.sh), both are on port 8000 and
# this variable is not needed — omit it or leave it empty.
NEXT_PUBLIC_API_URL=http://localhost:8000
